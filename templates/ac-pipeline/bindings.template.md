---
name: ac-bindings
description: Project bindings for the AC pipeline skills — tracker, branching, ship policy. Generated by /installAC; edit freely.
---

- **Tracker**: {{TRACKER}}
  - jira: intake via `jira` MCP tools (`jira_get_issue`, `jira_search`); the
    project must have the server registered.
  - github: intake via `gh issue view/list`.
  - none: /ac takes a described goal; task-id is a slug.
- **Base branch**: {{BASE_BRANCH}} — always `git fetch origin` and
  branch from `origin/{{BASE_BRANCH}}`.
- **Ship policy**: {{SHIP_POLICY}}
- **Done-transition**: {{DONE_TRANSITION}} — attended-only, never unattended.
- **Standards docs**: <optional paths the plan/QA/review phases must read>

<!-- ac-pipeline-version: 1 -->
